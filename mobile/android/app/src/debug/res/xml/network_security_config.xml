<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only: permits plain HTTP to the local dev backend (10.0.2.2 is
  the Android emulator's alias for the host machine's localhost; the
  physical-device case goes through `adb reverse`, also on 127.0.0.1).
  Production always talks to a real HTTPS API — this file is never
  merged into a release build (see src/debug/AndroidManifest.xml).
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
